
cloudstream {
    // All of these properties are optional, you can still build a
    // working plugin without them.
    version = 1
    description = "A provider for Invidious"
    language = "en"
    authors = listOf("tauquir-mta")

    // The status of the plugin
    // 1 = Down, 2 = Ok, 3 = Slow, 4 = Beta
    status = 2
}
